modLoader = "kotlinforforge" #mandatory
loaderVersion = "${loader_version_range}"
license = "${mod_license}"

issueTrackerURL="https://github.com/Evoloxi/Taurine/issues"

[[mods]]
modId = "${mod_id}"
version = "${mod_version}"
displayName = "${mod_name}"
displayURL = "https://github.com/Evoloxi" #optional
#logoFile="taurine.png" #optional
#credits="" #optional
authors = "${mod_authors}" #optional

description = '''${mod_description}'''

[[mixins]]
config = "${mod_id}.mixins.json"

# The [[accessTransformers]] block allows you to declare where your AT file is.
# If this block is omitted, a fallback attempt will be made to load an AT from META-INF/accesstransformer.cfg
#[[accessTransformers]]
#file="META-INF/accesstransformer.cfg"

[[dependencies."${mod_id}"]]
modId = "forge"
mandatory = true
versionRange = "${forge_version_range}"
ordering = "NONE"
side = "CLIENT"

[[dependencies."${mod_id}"]]
modId = "minecraft"
mandatory = true
versionRange = "${minecraft_version_range}"
ordering = "NONE"
side = "CLIENT"

[[dependencies."${mod_id}"]]
modId = "vanillin"
mandatory = true
versionRange = "[${vanillin_version},)"
ordering = "NONE"
side = "CLIENT"

[[dependencies."${mod_id}"]]
modId = "flywheel"
mandatory = true
versionRange = "[${flywheel_version},)"
ordering = "NONE"
side = "CLIENT"

[[dependencies."${mod_id}"]]
modId = "create"
mandatory = true
versionRange = "[${create_dependency_version},)"
ordering = "NONE"
side = "CLIENT"

#[features."${mod_id}"]
#openGLVersion="[3.2,)"
